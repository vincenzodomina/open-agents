/** Shown when the session uses {@link SandboxState.type} `"just-bash"` (no code-server). */
export const JUST_BASH_CODE_EDITOR_DISABLED_REASON =
  "Code editor is not available with the local just-bash sandbox—it requires code-server inside a full environment. Edit files here in the viewer or switch to a Vercel sandbox.";
